# postlude

Creation Classes of type (f a <-> a):
Pure <-> Default
Unit <-> Empty

Functor -> Apply |
            Pure -> Applicative
                 -> Monad
            Unit -> Alternative

Semigroup |
    Empty -> Monoid
